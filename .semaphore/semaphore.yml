version: v1.0
name: Hasura demo
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

  containers:
    - name: main
      image: semaphoreci/ubuntu:18.04
    - name: nginx
      image: nginx:latest

blocks:
  - name: "Hasura"
    task:
      jobs:
      - name: Test
        commands:
          - echo "here"
          - docker ps -a
          - checkout
          - curl http://nginx
