version: v1.0
name: Hello Docker

agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804

  containers:
    - name: main
      image: circleci/php:7.1-apache-jessie

blocks:
  - name: "Hello"
    task:
      jobs:
      - name: Hello
        commands:
          - apt-get install -y git
          - checkout
          - php --version
          - apache2 --version
